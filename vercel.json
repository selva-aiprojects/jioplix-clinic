{
  "version": 2,
  "outputDirectory": "apps/web/dist",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install --include=dev",
        "buildCommand": "mkdir -p apps/web/node_modules/@jioplix && cp -r packages/contracts apps/web/node_modules/@jioplix/contracts && cp -r packages/db apps/web/node_modules/@jioplix/db && cd packages/contracts && npm run build && cd ../db && npm run build && cd apps/web && npm run build && mkdir -p dist && cp -r apps/web/dist/* dist/"
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}